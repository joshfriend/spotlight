pluginManagement {
  plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
    id 'org.jetbrains.kotlin.jvm' version '2.1.20'
    id 'com.autonomousapps.testkit' version '0.13'
    id 'org.jetbrains.kotlinx.binary-compatibility-validator' version '0.17.0'
    id 'com.gradle.plugin-publish' version '1.3.1'
  }
}

plugins {
  id 'org.gradle.toolchains.foojay-resolver-convention'
}

dependencyResolutionManagement {
  repositoriesMode = RepositoriesMode.FAIL_ON_PROJECT_REPOS
  repositories {
    mavenCentral()
  }
}

rootProject.name = 'better-settings'

println("startparameter projectdir ${gradle.startParameter.projectDir}")
gradle.startParameter.taskRequests.forEach { req ->
  println("project ${req.projectPath} args ${req.args}")
}

include ':gradle-plugin'